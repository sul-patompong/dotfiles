archive/

{{ if ne .chezmoi.os "darwin" }}
Library/
.config/aerospace/
.config/karabiner/
.config/sketchybar/
.chezmoiscripts/darwin/**
{{ end }}

{{ if ne .chezmoi.os "linux" }}
# Linux-only paths will be listed here when added
{{ end }}

# lazygit uses XDG path on both OSes now; Library copy is redundant
Library/Application Support/lazygit/

# Workstation-only (GUI) configs — skip on servers
{{ if eq .machineType "server" }}
dot_config/ghostty/**
{{ end }}

# Generated files
dot_config/nvim/lazy-lock.json
