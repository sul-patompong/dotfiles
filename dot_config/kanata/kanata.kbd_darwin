(defcfg
  process-unmapped-keys yes
)

(defsrc
  f1  f2  f3  f4  f5  f6  f7  f8  f9  f10 f11 f12
  caps
)

(defalias
  ;; Caps Lock - tap for input switch, hold for ctrl
  cap (tap-hold 200 200 caps lctl)
)

(deflayer base
  brdn brup f3 f4 f5 f6 prev pp next mute vold volu
  @cap
)
